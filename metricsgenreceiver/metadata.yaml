type: metricsgen

status:
  class: receiver
  stability:
    development: [metrics, logs]
  distributions: [contrib]
  codeowners:
    active: [felixbarny, ruflin]

tests:
  config:
    interval: 30000